import styled from 'styled-components';

export const StyledSolveOptions = styled.div`
    position: relative;
    left: 50%;
    transform: translateX(-50%);

    label {
        display: block;
    };
`;
