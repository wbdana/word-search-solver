import * as React from 'react';
import { StyledHeader } from '../styled/header';

function Header() {
    return (
        <StyledHeader>
            Word Search Solver
        </StyledHeader>
    );
};

export default Header;
